#include <iostream>
#include <vector>
#include <map>
using namespace std;
int main(){
    int N;
    map<string, vector<strin> city_to_state = {};
    map<string, vector<string>> state_to_cities = {}; 
    for (int i=0; i<N; i++){
        string city, state; cin >> city >> state;
        state_to_cities[state] = state_to_cities[state];
        state_to_cities[state].push_back(city);;
        city_to_state[city].
    }
}